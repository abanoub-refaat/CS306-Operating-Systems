ORG 100H
 MOV AL, DATA[0]
 MOV COPY[0], AL

 MOV SI, 0
 MOV AL, DATA[SI]
 MOV COPY[SI], AL

 INC SI ; SI = 1
 MOV SI, 0
 MOV AL, DATA[SI]
 MOV COPY[SI], AL

 INC SI ; SI = 2
 MOV SI, 0
 MOV AL, DATA[SI]
 MOV COPY[SI], AL

 INC SI ; SI = 3
 MOV SI, 0
 MOV AL, DATA[SI]
 MOV COPY[SI], AL

 INC SI ; SI = 4
 MOV SI, 0
 MOV AL, DATA[SI]
 MOV COPY[SI], AL

 INC SI ; SI = 5
 MOV SI, 0
 MOV AL, DATA[SI]
 MOV COPY[SI], AL


RET
DATA DB 25h, 4Fh, 85h, 1Fh, 2Bh, 0C4h
COPY DB ? DUP(6)